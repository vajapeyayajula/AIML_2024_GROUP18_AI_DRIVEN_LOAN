version: '2.0'

services:
  web:
    build: 
      context: .
      dockerfile: ./src/Dockerfile
    command: uvicorn src.app.main:app --reload --workers 1 --host 0.0.0.0 --port 8000
    volumes:
      - .:/usr/src/app/
    ports:
      - 8002:8000
